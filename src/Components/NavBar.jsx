import { Box } from "@chakra-ui/react";
import { themeConfig } from "../utils/themeConfig";

function NavBar() {
  return (
    <Box bg={themeConfig.NavbgColor}>
      <h1>Hello here will come the nabar</h1>
    </Box>
  );
}

export default NavBar;
